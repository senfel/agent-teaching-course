# 第 04 期 · 知识卡片:System Prompt 人设

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│      给 Agent 戴一顶"帽子"——它就不是通用助手了               │
│                                                             │
│   ┌─────────────────────────────────┐                       │
│   │ templates/SOUL.md               │                       │
│   │  你是 Alex,技术助理...          │  ← 人设文件            │
│   │  能力边界: 擅长 Python...       │                       │
│   └─────────────────────────────────┘                       │
│          │                                                   │
│          ▼ 加载为 messages[0]                               │
│   ┌─────────────────────────────────┐                       │
│   │ messages[]                      │                       │
│   │  ├ system: "你是 Alex..."       │  ← 全局指令            │
│   │  ├ user: "你是谁?"              │                       │
│   │  └ assistant: "我是 Alex..."    │                       │
│   └─────────────────────────────────┘                       │
│          │                                                   │
│          ▼ 每次调用都读 system                              │
│   ┌─────────────────────────────────┐                       │
│   │  Alex: 我是 Alex,技术助理 ✓    │  ← 有身份了            │
│   └─────────────────────────────────┘                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 本期三个关键动作

1. **写人设文件** — templates/SOUL.md,定义角色、能力边界、回答风格
2. **加载为 messages[0]** — `{"role": "system", "content": ...}`,全程约束每次调用
3. **截断时保护 system** — trim_history 跳过 messages[0],人设永不被删

## 一行代码记住本期

```python
messages = [{"role": "system", "content": system_prompt}]  # ← 第一条是人设
```

> 把人设作为 messages[0] 注入,每次调用模型都先读 system——就像戴了一顶"帽子"。

## 有无 system prompt 的行为对比

同一个问题"你好,你是谁?":

| | 无 system prompt | 有 system prompt(Alex) |
|---|---|---|
| 自我认知 | "我是由 XX 训练的大语言模型" | "我是 Alex,技术助理" |
| 回答风格 | 通用、客套、列一堆方向 | 简洁、务实、聚焦技术 |
| 能力边界 | 什么都接 | 非技术问题礼貌引导回技术 |

> 不是改了模型,是给模型加了边界——system 消息的优先级高于普通 user 消息。

## 截断逻辑的变化

```
第 03 期:messages 从头删  → 没问题(无 system)
第 04 期:跳过 system 只删对话 → 人设永不丢
```

> 无论聊多少轮,messages[0] 永远是 system prompt,Alex 永不丢身份。

## 四期演进

| 期 | messages 结构 | Agent 能力 |
|---|---|---|
| 01 | `[user]` | 单次调用 |
| 02 | `[user]`(每次独立) | 循环对话(无记忆) |
| 03 | `[user, assistant, ...]` | 有短期记忆 |
| **04** | **`[system, user, assistant, ...]`** | **有记忆 + 有身份** |

## 一句话总结

> system prompt 是 messages[0],模型每次调用都先读它——就像给 Agent 戴了一顶"帽子",戴着这顶帽子,它就不是通用助手了,而是 Alex 技术助理。
